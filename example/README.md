# kw_amap_search example

This app demonstrates the full plugin flow:

- Set Android and iOS AMap keys
- Apply the privacy sequence before search
- Run keyword POI search
- Run nearby POI search with a configurable radius
- Display normalized POI results and native SDK errors

Run it from this directory:

```bash
flutter run
```
